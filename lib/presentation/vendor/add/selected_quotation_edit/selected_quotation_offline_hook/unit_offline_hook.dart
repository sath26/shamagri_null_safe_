import 'package:flutter/widgets.dart';

class UnitDropDownOfflineHook extends StatelessWidget {
  const UnitDropDownOfflineHook(num quotationIndex, {Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
